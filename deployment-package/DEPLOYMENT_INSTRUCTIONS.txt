🚀 MANUAL DEPLOYMENT INSTRUCTIONS
================================

Your Personal Finance Management App is ready for deployment!

📦 DEPLOYMENT PACKAGE: deployment-package/backend
🌐 TARGET SERVER: 16.176.9.42 (AWS EC2)
🎯 LIVE SERVER URL: http://16.176.9.42:5001

📋 STEP-BY-STEP DEPLOYMENT:

1. CONNECT TO EC2 INSTANCE:
   ssh -i "Finance Management.pem" ubuntu@16.176.9.42

2. UPLOAD BACKEND FILES:
   - Upload the 'backend' folder from deployment-package to your EC2 instance
   - You can use SCP, SFTP, or copy-paste the files

3. NAVIGATE TO BACKEND DIRECTORY:
   cd backend

4. INSTALL DEPENDENCIES:
   npm ci

5. VERIFY ENVIRONMENT FILE:
   - Ensure .env file exists with:
     MONGO_URI=mongodb+srv://swethadonthi:5mU4mYRVGNuKSp2c@cluster0.lp7otm9.mongodb.net/personal-finance-management-app?retryWrites=true&w=majority&appName=Cluster0
     JWT_SECRET=73RVFkY/xM0J/o7keLdIYmajCjjXY8pXZnopebJwpew=
     PORT=5001

6. TEST THE APPLICATION:
   npm test

7. INSTALL PM2 (if not already installed):
   npm install -g pm2

8. START THE APPLICATION:
   pm2 start server.js --name personal-finance-backend

9. SAVE PM2 CONFIGURATION:
   pm2 save

10. CHECK STATUS:
    pm2 list
    pm2 logs personal-finance-backend

🌐 YOUR LIVE API ENDPOINTS:

Base URL: http://16.176.9.42:5001

Authentication:
- POST /api/auth/register - User registration
- POST /api/auth/login - User login

Transactions:
- GET /api/transactions - Get all transactions
- POST /api/transactions - Create transaction
- PUT /api/transactions/:id - Update transaction
- DELETE /api/transactions/:id - Delete transaction

Budgets:
- GET /api/budgets - Get all budgets
- POST /api/budgets - Create budget
- PUT /api/budgets/:id - Update budget
- DELETE /api/budgets/:id - Delete budget

Savings Goals:
- GET /api/savings-goals - Get all savings goals
- POST /api/savings-goals - Create savings goal
- PUT /api/savings-goals/:id - Update savings goal
- DELETE /api/savings-goals/:id - Delete savings goal

🔧 TROUBLESHOOTING:

If SSH connection times out:
- Check EC2 security groups
- Ensure port 22 is open for SSH
- Verify the PEM file permissions

If application doesn't start:
- Check if port 5001 is open in security group
- Verify MongoDB connection
- Check PM2 logs: pm2 logs personal-finance-backend

If PM2 is not installed:
- Run: npm install -g pm2

✅ SUCCESS INDICATORS:
- PM2 shows "online" status
- No errors in PM2 logs
- API responds at http://16.176.9.42:5001

🎉 Your Personal Finance Management API will be live and ready to use!
